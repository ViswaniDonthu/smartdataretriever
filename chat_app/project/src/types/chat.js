// Chat data structures and types for JavaScript

export const MessageSender = {
  USER: 'user',
  AI: 'ai'
};

export const Theme = {
  LIGHT: 'light',
  DARK: 'dark'
};

// Example structures (for documentation):
// Message: { id, content, sender, timestamp }
// ChatSession: { id, title, messages, createdAt, updatedAt }